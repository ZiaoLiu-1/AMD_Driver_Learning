---
description: 实施计划 — 拆成 2–5 分钟小任务，精确路径与命令（obra/superpowers）
alwaysApply: false
---

# Writing Plans (Superpowers)

源自 [obra/superpowers](https://github.com/obra/superpowers)。多步骤任务、有 spec 时，写计划再动手。

## 原则

假设执行者零上下文。每步 2–5 分钟，DRY、YAGNI、TDD、频繁 commit。

## 任务结构

- **精确路径**：`client/src/foo.ts:12-20`
- **完整代码**：不写「加校验」，写具体代码
- **精确命令**：`pnpm test path/to/test.ts` + 预期输出
- **每步单动作**：写失败测试 → 跑确认失败 → 写实现 → 跑确认通过 → commit

## 保存

`docs/plans/YYYY-MM-DD-<feature>.md`
