---
description: PR 与版本控制规范 — 遵循行业标准的 Pull Request 管理流程
alwaysApply: true
---

# PR 与版本控制规范

本规则确保所有 Git 操作、分支管理和 Pull Request 流程符合行业最佳实践。详见 `BRANCH_STRATEGY.md`。

## 分支命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 功能 | `feature/<简短描述>` | `feature/search-modal`, `feature/i18n` |
| 修复 | `fix/<问题简述>` | `fix/nav-404`, `fix/locale-switch` |
| 文档 | `docs/<主题>` | `docs/api-readme` |

- 使用小写字母和连字符
- 名称简短且语义明确

## 提交信息 (Conventional Commits)

```
<type>(<scope>): <subject>

[可选的 body]
```

**type**: `feat` | `fix` | `docs` | `style` | `refactor` | `test` | `chore`

**示例**:
- `feat(search): add bilingual search with highlight`
- `fix(i18n): correct locale switcher for subpath deploy`
- `chore(deps): bump react to 19.2`

- 第一行 ≤ 72 字符
- 使用祈使语气（add 而非 added）
- 英文撰写

## PR 标题与描述

**标题**: 与提交信息一致，可加 PR 编号
```
feat(search): add bilingual search with highlight (#42)
```

**描述模板**（必须包含）:
```markdown
## 变更概要
简要说明做了什么。

## 类型
- [ ] feat / fix / docs / refactor / chore

## 自测
- [ ] 本地 `pnpm run check` 通过
- [ ] 本地 `pnpm run test` 通过
- [ ] 手动验证关键路径

## 相关
- 关联 issue（如有）
```

## PR 流程

1. **创建 PR 前**: 从 `develop` 拉取最新并 rebase
   ```bash
   git checkout develop && git pull
   git checkout feature/xxx && git rebase develop
   ```

2. **目标分支**:
   - 常规功能: `feature/*` → `develop`
   - 发布: `develop` → `main`
   - 紧急修复: `fix/*` → `main`（跳过 develop）

3. **合并前**: CI 必须全部通过；至少一人 review（团队规模允许时）

4. **合并方式**: 优先 **Squash and merge**，保持 main/develop 历史线性

## 禁止操作

- 禁止直接 push 到 `main`（除 hotfix 的 PR merge）
- 禁止 `git push --force` 到共享分支
- 禁止在 PR 未合并时删除源分支

## 冲突处理

- 冲突时优先 `git rebase origin/develop`，保持提交历史清晰
- 解决后 `git push --force-with-lease` 更新 PR 分支（仅限自己的 feature/fix 分支）
