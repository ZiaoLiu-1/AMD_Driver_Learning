---
description: UI/UX 专业规范 — 预交付清单、图标/交互/对比度（nextlevelbuilder/ui-ux-pro-max-skill）
globs: client/**/*.tsx, client/**/*.css
alwaysApply: false
---

# UI/UX Pro Max

源自 [nextlevelbuilder/ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill)。设计/构建/审查 UI 时参考。

## 预交付清单

### 视觉
- [ ] 不用 emoji 做图标（用 SVG：Heroicons/Lucide）
- [ ] 图标统一来源
- [ ] Hover 不导致 layout shift
- [ ] 直接用主题色（如 `bg-primary`）

### 交互
- [ ] 可点击元素加 `cursor-pointer`
- [ ] Hover 有清晰反馈
- [ ] 过渡 150–300ms
- [ ] 键盘 focus 可见

### 明暗模式
- [ ] 亮色模式文字对比 ≥ 4.5:1
- [ ] 玻璃/透明元素在亮色下可见
- [ ] 边框在两种模式下可见

### 布局
- [ ] 浮动元素与边缘有间距
- [ ] 内容不被固定 navbar 遮挡
- [ ] 响应式：375 / 768 / 1024 / 1440px
- [ ] 移动端无横向滚动

### 可访问性
- [ ] 图片有 alt
- [ ] 表单有 label
- [ ] 不只靠颜色传达信息
- [ ] 尊重 `prefers-reduced-motion`

## 常见规则

| 规则 | 做 | 不做 |
|------|----|------|
| 图标 | SVG (Lucide/Heroicons) | emoji 做图标 |
| 光标 | 可点击加 `cursor-pointer` | 默认光标 |
| 过渡 | `transition-colors duration-200` | 瞬间切换或 >500ms |
| 亮色玻璃 | `bg-white/80` 或更高 | `bg-white/10` |
| 正文文字 | `slate-900` | `slate-400` 做正文 |
| 边框 | `border-gray-200` 亮色 | `border-white/10` 亮色 |

## 优先级

1. **Accessibility** — 对比度、focus、aria、键盘
2. **Touch** — 44×44px 触控、loading 态、错误反馈
3. **Performance** — 图片优化、reduced-motion、防内容跳动
4. **Layout** — viewport、字号、z-index 层级
