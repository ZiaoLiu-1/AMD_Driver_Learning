---
description: Web 界面规范 — 可访问性、性能、UX 审计，100+ 规则
globs: client/**/*.tsx, client/**/*.css
alwaysApply: false
---

# Web Interface Guidelines

源自 [vercel-labs/web-interface-guidelines](https://github.com/vercel-labs/web-interface-guidelines)。审查 UI、可访问性、设计时参考。

## 可访问性

- 图标按钮需 `aria-label`
- 表单控件需 `<label>` 或 `aria-label`
- 交互元素需键盘支持 (`onKeyDown`/`onKeyUp`)
- 图片需 `alt`（装饰性用 `alt=""`）
- 装饰性图标用 `aria-hidden="true"`
- 异步更新（toast、校验）需 `aria-live="polite"`
- 优先语义 HTML (`<main>`, `<nav>`, `<article>`) 再 ARIA
- 标题层级 H1–H6；含 skip link

## 焦点

- 交互元素需可见 focus：`focus-visible:ring-*`
- 避免 `outline-none` 且无替代
- 用 `:focus-visible` 而非 `:focus`

## 表单

- 输入需 `autocomplete` 和有意义 `name`
- 正确 `type` 和 `inputmode`
- 不阻止粘贴 (`onPaste` + `preventDefault`)
- label 可点击 (`htmlFor` 或包裹控件)
- 错误内联显示，聚焦首个错误

## 动画

- 尊重 `prefers-reduced-motion`
- 用 `transform`/`opacity`（compositor-friendly）
- 避免 `transition: all`，显式列出属性

## 排版

- 用 `…` 而非 `...`
- 用弯引号 `"` `"` 而非直引号
- 数字列用 `font-variant-numeric: tabular-nums`

## 图片

- `<img>` 需明确 `width`/`height`（防 CLS）
- 首屏外图片 `loading="lazy"`
- 首屏关键图 `fetchpriority="high"`

## 性能

- 长列表 (>50) 虚拟化或 `content-visibility: auto`
- 不在 render 中读 layout 尺寸
- 批量 DOM 读写，避免交错

## 暗色模式

- `color-scheme: dark` 在 `<html>` 上
- `<meta name="theme-color">` 匹配背景

## i18n

- 日期用 `Intl.DateTimeFormat`
- 数字/货币用 `Intl.NumberFormat`

## 反模式（需避免）

- `user-scalable=no` 禁用缩放
- `onPaste` + `preventDefault`
- `transition: all`
- `outline-none` 且无 focus-visible 替代
- 用 `<div>` 做可点击导航（应使用 `<a>`）
- 图片无尺寸
- 大数组 `.map()` 无虚拟化
